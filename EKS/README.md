# Install eksctl
```

eksctl create cluster --name cluster2 --nodegroup-name ng1 --region us-east-1 --zones=us-east-1a,us-east-1b --node-type t2.micro --nodes 2 --profile murthy

kubectl config view

```